---
"@analytics-kit/react": minor
---

Chart components now use Tailwind + Recharts with visual `variant`s (gradient, step, horizontal, donut…). Color themes were removed; charts inherit --chart-1…5 from the host site.
