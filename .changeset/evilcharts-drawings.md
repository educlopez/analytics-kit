---
"@analytics-kit/react": minor
---

Charts gain EvilCharts-inspired drawings: area `hatched` / `bars` / `solid`, line `ping` / `rainbow` / `values`, bar `glow` / `gradient` / `duotone`, pie `rounded` / `radial` / `glow`.
