---
"@wingtics/connector-mock": patch
---

The mock connector reported `analytics-kit-demo.vercel.app` as its site name, which the demo dashboard shows verbatim — so the old brand was still on screen. It now reports `wingtics.com`.
